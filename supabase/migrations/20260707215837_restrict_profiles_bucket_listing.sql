-- الـ bucket "profiles" عام (public = true) بالفعل، فأي كائن بداخله يظل قابل
-- للعرض عبر رابطه العام (getPublicUrl) بغض النظر عن سياسات RLS هذه — السياسات
-- هنا تتحكم فقط في إمكانية استخدام واجهة الـ Storage API (مثل list()) لسرد كل
-- الملفات الموجودة في الـ bucket. لا يوجد أي استخدام لـ list() في كود الواجهة
-- (فقط getPublicUrl)، فحذف سياسة القراءة العامة هنا آمن تماماً ولا يكسر عرض
-- الصور الشخصية، ويمنع فقط إمكانية سرد/عد كل الملفات عبر الـ API.
DROP POLICY IF EXISTS "avatars_public_read" ON storage.objects;
;
