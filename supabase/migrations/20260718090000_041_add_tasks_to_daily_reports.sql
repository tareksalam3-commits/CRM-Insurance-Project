-- إضافة عمود المهام (tasks) لجدول daily_reports — جزء النشاط الشخصى الخاص
-- بدرجة "المراقب" مختلف عن باقي الدرجات: بدلاً من المواعيد، يُدخل المراقب
-- مهام (اسم المهمة + وصفها). العمود فارغ ([]) لكل الدرجات الأخرى، ويبقى
-- اختيارياً حتى للمراقب نفسه — لو لم يُضِف مهام يظل فارغاً ولا يظهر قسمه
-- فى التقرير المطبوع (يُتحكم بهذا فى الواجهة، لا فى قاعدة البيانات).

ALTER TABLE daily_reports
    ADD COLUMN IF NOT EXISTS tasks jsonb NOT NULL DEFAULT '[]'::jsonb;
